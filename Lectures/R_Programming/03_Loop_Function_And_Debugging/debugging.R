message("dupa")

warning("dupa") #printed after the exectution of function

error("kupa")


x
invisible(x) # prevents auto printing, returns x

str(traceback) # stack of the last error

str(recover) # stops on error, can be done with > options(error = recover)

str(debug) # stops at function and opens browser (debugger)

str(browser) # like a breakpoint

str(trace)


